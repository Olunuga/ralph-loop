# OS
.DS_Store

# Plugin development
node_modules/

# Runtime state (written by loop.sh, must not be committed)
ralph/.loop_status
ralph/.loop_output
ralph/.fix_agent.log

# ralph-bridge schema validation copy (recreate with: cp -R schemas/ralph-bridge openspec/schemas/)
openspec/schemas/
